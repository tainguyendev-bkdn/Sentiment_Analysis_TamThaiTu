# Database Configuration (PostgreSQL)
# TODO: Update these values with your actual database credentials

db.url=jdbc:postgresql://localhost:6969/admin
db.username=admin
db.password=123456
db.driver=org.postgresql.Driver

# HikariCP Connection Pool Configuration
db.pool.maximum=10
db.pool.minimum.idle=5
db.pool.connection.timeout=30000
db.pool.idle.timeout=600000
db.pool.max.lifetime=1800000

# Application Configuration
app.name=Sentiment MVC
app.version=1.0.0

# Worker Thread Configuration
worker.thread.count=3
worker.queue.size=100

# TODO: Add other configuration properties as needed

